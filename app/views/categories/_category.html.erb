<li>
	<%= link_to category do %>
		<% if !current_page?(root_path) && (category.id == @category.id) %>
		<div class='row category selected' id="category-<%= category.id %>">
		<% else %>
		<div class='row category' id="category-<%= category.id %>">
		<% end %>
			<hr />
	  	<span><%= category.title %></span><i class="fa <%= category.mobile_icon %> fa-5x hidden-md hidden-lg category-icon"></i><i class="fa fa-chevron-right category-arrow"></i>
	  </div>
	<% end %>
</li>